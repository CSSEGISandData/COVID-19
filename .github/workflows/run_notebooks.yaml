name: Update data
on:
  schedule:
    - cron:  '*/15 * * * *'
  push:
    branches:
      - master

jobs:
  update-notebooks:
    runs-on: ubuntu-latest
    steps:
    
    - name: Copy Repository Contents
      uses: actions/checkout@v2

    - name: Run notebooks
      run: |
        pip3 install pandas scrapy jupyterlab papermill ipykernel
        python3 -m ipykernel install --user --name python3
        sudo chmod -R 777 .
        cd data_generation
        ls *.ipynb